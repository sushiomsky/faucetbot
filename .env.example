# DuckDice Faucet Bot Configuration
# Copy this file to .env and fill in your details
# Never commit the .env file to version control!

# Your DuckDice API Key (required)
# Get it from: https://duckdice.io (Account Settings -> Bot API)
DUCKDICE_API_KEY=your-api-key-here

# API Base URL (optional, use default if not specified)
# DUCKDICE_BASE_URL=https://duckdice.io/api

# Request timeout in seconds (optional, default: 30)
# DUCKDICE_TIMEOUT=30

# Progressive Win Chance Strategy (Faucet Mode)
# Faucets are rolled all-in with increasing win chances:
# 1st faucet: base_win_chance (0.01%)
# 2nd faucet: base_win_chance + increment (0.02%)
# 3rd faucet: base_win_chance + 2*increment (0.03%)
# And so on...

# Base win chance percentage for first faucet (default: 0.01)
# FAUCET_BASE_WIN_CHANCE=0.01

# Win chance increment per faucet (default: 0.01)
# FAUCET_WIN_CHANCE_INCREMENT=0.01

# Minimum USD value to trigger cashout from faucet to main balance
# Default: 20.0 (only cashout when faucet balance >= $20 USD equivalent)
# FAUCET_CASHOUT_MIN_USD=20.0

# Withdrawal wallet address (required for auto-withdrawal)
# WITHDRAWAL_ADDRESS=your-wallet-address-here

# Enable auto-withdrawal after cashout (default: false)
# Set to "true" to automatically withdraw after faucet cashout
# AUTO_WITHDRAW=false

# Minimum USD value to trigger auto-withdrawal
# Default: 20.0 (only withdraw when main balance >= $20 USD equivalent)
# WITHDRAWAL_MIN_USD=20.0

# Delay between API requests in milliseconds (default: 1000)
# REQUEST_DELAY_MS=1000

# ============================================
# Normal Mode Configuration (Junkhead Strategy)
# ============================================
# Bet size is calculated as a percentage of balance (not fixed amount)
# Uses a mixed approach: frequent low-risk bets + occasional high-risk bets

# Low-Risk Strategy Settings (~2.0x payout)
# Target win chance for low-risk bets (default: 49.5%, ~2.0x payout)
# NORMAL_LOW_RISK_WIN_CHANCE=49.5

# Bet size as percentage of balance for low-risk bets (default: 5.0%)
# NORMAL_LOW_RISK_BET_PERCENT=5.0

# High-Risk "Slayer" Strategy Settings (~8.2x payout)
# Target win chance for high-risk bets (default: 12.0%, ~8.2x payout)
# NORMAL_HIGH_RISK_WIN_CHANCE=12.0

# Bet size as percentage of balance for high-risk bets (default: 1.0%)
# NORMAL_HIGH_RISK_BET_PERCENT=1.0

# How often to place high-risk bets (default: every 5 bets, 0 to disable)
# NORMAL_HIGH_RISK_FREQUENCY=5

# Session Management
# Maximum number of bets per session (default: 50)
# NORMAL_MAX_BETS=50

# Stop loss percentage - stop if balance drops by this amount (default: 50%)
# NORMAL_STOP_LOSS_PERCENT=50

# Take profit percentage - stop if balance increases by this amount (default: 50%)
# NORMAL_TAKE_PROFIT_PERCENT=50
